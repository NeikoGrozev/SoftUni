<section id="viewhotelDetails">
    <h2>Details</h2>
    <div class="hotel-ticket">
        <div class="hotel-left">
            <img src="{{hotelData.imageUrl}}" alt="">
        </div>
        <div class="hotel-right">
            <div>
                <h3>{{hotelData.name}}</h3>
            </div>
            <div>
                {{hotelData.city}}
            </div>
            <p><span>Free rooms: {{hotelData.rooms}}</span> </p>

            {{#if hasUser}}

                {{#if isOwner}}
                    <a href="/hotels/edit/{{hotelData._id}}" class="edit">Edit</a>
                    <a href="/hotels/delete/{{hotelData._id}}" class="remove">Delete</a>
                {{else}}
                    {{#if isBooked}}
                        <p><span class="green">You already have booked a room</span> </p>
                    {{else}}
                        <a href="/hotels/book/{{hotelData._id}}" class="book">Book</a>
                    {{/if}}
                {{/if}}
            {{/if}}
        </div>

    </div>
</section>